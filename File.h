#pragma once
/*#include "User.h"
#include "Test.h"
#include <fstream>
#include <vector>
class File
{
public:
	void loadUsers(User**&);
	//int getUsersCount();
	bool createUser(User*,UserType);
	bool loadUser(User*&, std::string, std::string);
	//bool saveUser(User*);
private:
	bool isLoginFree(std::string);
};*/